[alias]
	st = status -sb
	today = log --since=midnight --author='Kosuke Kusano' --oneline
	c = commit
    log-better = log --all --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
[color]
	ui = auto
[user]
	name = koku
	email = cocuh.kk@gmail.com
[github]
	user = cocuh
[core]
	autocrlf = input
	pager=less -q -R 
	excludesfile = ~/.gitignore
[merge]
	tool = vimdiff
#[push]
#	default = simple
[hub]
    user = cocuh
    protocol = https
